---
description: "[USER] Profile this project's bug-loop tooling (issue tracker / observability / reproduce) into a routing-only, secret-free apex.profile.toml — probes deps, CI configs, connected MCP servers, and the git remote; auto-fills what it can infer, asks for the rest"
---

Invoke the `detect-stack` skill from the apex plugin for this task. Read its SKILL.md and follow it.
