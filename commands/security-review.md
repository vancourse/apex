---
description: "[AUTO] PR-time security audit — 5-pass: secrets, authn + authz (per-layer, fail-closed), input validation + output encoding, dependency vuln + supply chain, audit log + observability for security events. Plus adversarial counter-pass."
---

Invoke the `security-review` skill from the apex plugin for this task. Read its SKILL.md and follow it.
