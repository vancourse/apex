---
description: "[USER] Reconnaissance brief before design — enumerate the authoritative primitives, contracts, and invariants in the change's blast radius and emit a facts brief, so the design uses what already exists instead of reconstructing it"
---

Invoke the `recon` skill from the apex plugin for this task. Read its SKILL.md and follow it.
