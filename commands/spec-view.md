---
description: "[USER] Render a PRD / ADR set / design doc as a disposable, offline, rich HTML view for human freeze-review — color-coded freeze-readiness dashboard, inline-SVG diagrams, collapsible passes, severity badges, highlighted code. Markdown stays canonical; the HTML is a throwaway view (gitignored, never re-ingested)."
---

Invoke the `spec-view` skill from the apex plugin for this task. Read its SKILL.md and follow it.
