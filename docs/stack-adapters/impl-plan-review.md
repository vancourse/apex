_(verbatim Read-shadowed citations — every file:line below re-verified against HEAD this session)_

This is the `apex:impl-plan-review` synthesizer pass over the six lenses (Pass 1–5 + the cold adversarial counter-pass). I re-ran each load-bearing citation against the live tree rather than trusting the reviewers' or the plan's assertions. Verdict: **2 freeze-blockers confirmed, both fixed below; the plan is otherwise the strongest in the autonomous-fix/incident-retro ballpark.** Net `freeze_ready=true` after the fixes are applied (they are folded into `final_impl_plan_md`).

## Per-pass verdict (consolidated)

**Pass 1 — Layered PR stack: CONDITIONAL PASS → PASS after fix.** Stack *shape* is right: 4 build PRs + L-W + deferred L-D, real architectural seams, both split decisions argued. Stack *sizing* was the finding. I confirmed Review 1's calibration against HEAD: `skills/autonomous-fix/reference/conformance-lint.py` is **505 LOC**, while the same author's `docs/autonomous-fix/impl-plan.md:72` estimated it at **~150 LOC** — a **3.4× miss** on the structurally-analogous Python file (value-shape validators + error codes + checks). The same plan estimated `SKILL.md` at ~370 (`impl-plan.md:35,51`); it shipped at **234** (−37%). So the author's LOC method is unreliable in *both* directions, and the dominant file (the validator-bearing Python) is under-estimated. The plan budgets `profile.py` at ~250 carrying 3 dataclasses + a 4-step loader + **7** field-kind validators + 2 writer invariants + atomic write — denser than the 505-line precedent's 7 checks. **PR-A1 as scoped almost certainly breaches 400.** Same logic on PR-B1 (resolver 80 + contracts 70 + 2 adapters 180 + helper = ~380 *before* the gh exit-code→5-error-code mapping and grep window-parse/confine/escape balloon). **Fix applied:** the relief-valve splits for PR-A1 (`shapes.py` peel) and PR-B1 (adapters peel, the resolver-doesn't-depend-on-adapters seam the plan itself names in §6 Pass 1) are promoted from "flag-and-hope" to **pre-committed conditional splits with a stated trigger** (`>340 hand-written non-test LOC at author time → take the peel before opening the PR`), and every `~NNN LOC` header now carries `(estimate; re-measure at author time — the analogous conformance-lint.py shipped at 505 vs a ~150 plan estimate, so treat Python budgets as floors)`.

**Pass 2 — Sequencing / dependency order: PASS (0 blockers).** Re-verified: DAG acyclic, every PR states deps + unblocks, wiring last, count-bump co-located with the command in PR-A2. The plan is in fact *more current than design §G.4* on PR #25: I confirmed PR #25 is merged (`MAINTAINING.md` + `hooks/suggest-skill-on-edit.sh` live at HEAD), so the design's "if PR #25 is still in flight, rebase onto it" conditional is stale and the plan correctly treats the registry as the live merge surface. The `--exclude-dir=.claude` guard is confirmed **load-bearing today** — there are **TWO** stale worktrees (`.claude/worktrees/wonderful-almeida-a10191/` AND `.claude/worktrees/stoic-pascal-9a2822/`), not one as the design/plan claim. NITs (take the A2∥B1 parallelism as default; note L-D parallelizability; the hook-set is 9 scripts not 3) folded in as clarifications.

**Pass 3 — Test plan per layer (PRD↔mirror): PASS on load-bearing requirements (3 lineage NITs fixed).** Mirror complete both directions; all S1–S10 + sub-numbers have an owner; the four load-bearing tests (value-shape lint S1.2, adapter verbs S8, resolver S5.1/S5.2, S9 conformance) each have a named artifact + owner PR + correct `[unit]` tag; `repo_path` confinement correctly justified as a design-§C.2/F1 sub-assertion of S1.2, not gold-plating; `tracker.comment` correctly justified as S9 conformance-completeness. Finding 1 (S5's PRD-assigned `[gate-walk] + [unit]` compound tag silently re-partitioned into S5.1/S5.2 + S5-flow without stating the lineage) and the two NITs (S1.1 fixture-vs-detector-written seam; S3/S6 `[doc-assert]` legs are grep-asserts, not pytest in `test_detect.py`/`test_handoff.py`) are one-line lineage clarifications, all applied to §3.

**Pass 4 — Rollout: PASS (clean).** Re-verified against HEAD: CHANGELOG head is `## [0.3.7]` with **no `## [Unreleased]`**; `scripts/release.sh:59` hard-requires `## [Unreleased]`, `:67` aborts on a pre-bumped version, `:83` dates+renames, `:79` bumps `marketplace.json` too. Plan's "create fresh `## [Unreleased]`, don't pre-date, don't bump, release.sh does both version files" is exactly right. Versions both `0.3.7` → `0.3.8` correct. The single-`## [Unreleased]`-in-the-terminal-layer placement is a genuine merge-race-avoidance win. Two precision flags folded in: 4a (the `30+` decoy co-located on `README.md:59` — bump only the `~14`); 4b (`marketplace.json` second version site — already handled). The roll-forward-only release tag is the one irreversible artifact, correctly flagged.

**Pass 5 — Reversibility: FAIL-until-fixed → PASS after fix.** This pass found **BLOCKER B1** (the §H dropped mechanism) and two reversibility gaps (R1 reverse-coupling, R2 count-bump-not-freely-revertible), all confirmed against HEAD. The correct reversible structure (route each shared-file edit and its CHANGELOG/doc witness into the same revertible unit; declare top-down revert order) is applied to §5. The clean wins (release tag isolated, atomic temp-file+rename writer, apex-revert-touches-no-consumer-profile, clean-delete new dirs, two circuit breakers) all hold.

## Adversarial counter-pass (the cold attack lens, reconciled)

The two adversarial reviews (5 + 6) converge on the **same root cause** for the freeze-blockers: *the plan uses a false-or-half-true self-citation to scope an edit DOWN.* Both blockers are that pattern, and both are verified true at HEAD:

**BLOCKER B1 — the §H amendment is applied at 1 of 3 design-mandated sites; the plan's "no further gate-doc edit / nothing to revert" is false.** Design §H (`design.md:476`) is explicit: the carry-back is "a one-line note at **each of those three sites** (`prd.md:5`, `prd.md:154`, `SKILL.md:29`)." Live-tree check:
- `docs/autonomous-fix/prd.md:6` — the AMENDMENT 2026-06-07 block **exists** and is comprehensive (covers the `:5` region). ✓
- `docs/autonomous-fix/prd.md:146` (the design's drifted `:154`) — the **un-amended** Downstream line: *"investigate-bug (refactored to invoke this parent…)"* — no interpose note. ✗
- `skills/autonomous-fix/SKILL.md:29` — the **un-amended** *"they will be refactored to invoke it. Parent/child, not a parallel path."* — `grep -niE 'interpose|three-layer|stack-adapt'` over that SKILL.md returns **NONE**. ✗

So two design-mandated carry-back edits are absent, and they **actively contradict** the `:6` block (`:6` says generic interposes; `:29`/`:146` say direct "refactored to invoke it"). The reversibility consequence is real: a `git revert` of PR-B2 that removes a *newly-added* interpose note while leaving the prior-commit `:6` block produces an internally-contradictory gate PRD. The plan's "nothing to revert there" rests on the false premise that §H is complete.

**Adjudication:** B1 is a genuine freeze-blocker. The plan must take a position. Two options exist — (a) add the two missing one-liners as a cited build step with its own revert line, or (b) record an explicit deviation from design §H ("the `:6` block consolidates §H's three-site spread"). I rule for **(a)** because the live `SKILL.md:29` + `prd.md:146` text *actively contradicts* `:6` — leaving them is not "consolidation," it is two stale assertions a reader will hit. Per `~/.claude/CLAUDE.md` §1 ("scope changes after freeze require an explicit amendment, not a silent reinterpretation"), silently scoping §H down from 3 sites to 1 is exactly the prohibited move. **Fixed in `final_impl_plan_md`:** the two carry-back one-liners (`prd.md:146` region + `SKILL.md:29`) are added as an explicit, line-cited build step folded into **PR-B2** (it already owns the gate-doc reasoning), with its own §5 revert line and top-down revert-order annotation.

**BLOCKER B2 — the README Commands-table edit is demoted to a hedged "if the table enumerates rows" on a false reading of the tree.** Verified: `README.md` has a real Commands table — header at `:62` (`| Command | Backing skill | What it does |`), **13** `/apex:` rows (`:63-76`). The plan's fact block asserts "the README Commands prose at `:59` does not enumerate a per-command table row-for-row" — **FALSE.** Design §G.3 (`design.md:458-461`) is unhedged and explicit: *"the Commands table MUST gain a row or the 15-command menu has a 14-row table … not a hedged 'if the README carries a table'."* The 13-row table also confirms the pre-existing `/apex:adversarial-pair` gap (14 commands, 13 rows — `/apex:adversarial-pair` is absent from the table) the design and plan both rely on.

**Adjudication:** B2 is a freeze-blocker — two implementers provably diverge (one trusts the false fact-block + "if" and ships a 14-row table under a 15-command menu; one reads design §G.3 and adds the row). **Fixed:** the false `:59` claim is struck; the detect-stack Commands-table row is a **mandatory** PR-A2 edit per design §G.3, anchored "after the `/apex:setup` row at `README.md:76`" (the table's last row), with the explicit note that the table becomes 14-rows/15-menu (the adversarial-pair gap persists by exactly one, left as found).

**MAJOR (revise, non-blocking) — under-specified diagnosis heuristics (`derive_window` / `ids_from` / `paths_guess`).** Design §E.1/N3 explicitly *delegated* these to impl-plan ("PR-B specifies them; the design fixes only their contracts"). The plan restates only the *security contracts* (escaped literal, bounded) and gives zero buildable behavior — so `test_handoff.py` would assert against the author's improvisation. **Fixed:** PR-B2 now pins the three heuristics' MVP behavior (default bounded window span, `re.escape` extraction rule, stack-frame-intersect-tracked-files path mapping) so the gate-walk tests test a spec, not an improvisation. This discharges design §E.1's explicit delegation — leaving it unpinned would itself be a Pass-1 "a layer under-specified" failure.

**MAJOR (downgrade the claim, non-blocking) — the PR-A2 ∥ PR-B1 "legitimate parallelism" is overconfident.** Both root on PR-A1 and touch disjoint files, BUT both *import* `profile.py`; if either discovers a needed PR-A1 schema change, the parallelism collapses into a foundation-module merge conflict — which is precisely the contract-drift the plan's own freeze-section circuit breaker names. **Fixed:** the parallelism claim is reworded to "gated on PR-A1's schema being truly frozen (the S9 conformance test is the freeze-check, and it doesn't run until PR-B2)" and the deliberate serialization is kept.

**MINORs folded in:** two stale worktrees not one (design §G.1/§G.4 + plan say "7 sites in *a* worktree" — now "≥2 worktrees, re-check; the `--exclude-dir=.claude` guard handles all"); PR #25 is merged so "land the guard into PR #25" is dead — reframed to "the canonical `MAINTAINING.md:59-60` recipe still lacks `--exclude-dir=.claude`; add it as a 1-line registry-improvement co-located with the PR-A2 count-bump that needs it"; the hook-set fact block (3 named) corrected to 9 scripts at HEAD with the no-hook-added conclusion preserved.

## Where the plan was RIGHT (steelman — verified, not waved through)

- 6 canonical count-sites all read "14" at HEAD (`README.md:59,222,233`, `commands/help.md:10,61`, `WALKTHROUGH.md:100`); 14 command files. Bump targets exact.
- All gate seam lines true (`autonomous-fix/SKILL.md:29,35,36,57,65,73,79,96,176,191`).
- All FLOW.md anchors true (`:254,256,258,260,304,305`); help.md (`:10,24,26,37,39,61`); README Skills rows (`:52,53`).
- CHANGELOG/release.sh/version facts all true; `## [Unreleased]` absent; `0.3.7`→`0.3.8` correct.
- PR-A split at a real seam (data-contract vs heuristic-probe), matching design §K + the autonomous-fix L1/L2 precedent.
- PR-B resolver-first ordering with the S5.2 `mcp`-never-read exit gate faithfully encodes design §B.1's single-code-path guarantee.
- Test mirror complete both directions; `[unit]`/`[gate-walk]`/`[doc-assert]` predicate single-sourced; the negative-fixture→shape-leg map mirrors `conformance-lint.py`'s one-fixture-per-leg discipline.
- New dirs (`skills/detect-stack/`, `skills/investigate-bug/`, `tests/`) absent → clean-delete adds.

## Plan-freeze readiness

After applying the two blocker fixes (B1 §H two-site carry-back as a cited PR-B2 build step; B2 mandatory README Commands-table row) + the MAJOR pins (diagnosis heuristics; downgraded parallelism claim) + the Pass-1 pre-committed relief-valve splits + the Pass-3 lineage one-liners + the Pass-5 revert-order annotation + the MINOR fact corrections — **all 5 passes meet their pass conditions, the adversarial counter-pass is addressed, no blocker remains.** `freeze_ready = true`. The corrected plan is `final_impl_plan_md`.
